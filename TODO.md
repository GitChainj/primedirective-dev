# TODO

Deferred work, captured so it isn't lost.

## Main-site (primedirective.dev) changes — needs a dedicated change block

- **Add Platform Rule CTA to `primedirective.dev/organizations`.**
  Phase W2 (ADDITION 3) asked for a "Learn about the Platform Rule →" link on
  the Organizations page pointing to `https://conscience.wiki/platform-rule`.
  Skipped during W2 because the "primedirective.dev unchanged" constraint took
  precedence. Add it when a block explicitly scoped to main-site changes is
  opened (W3 or later). Insertion point: the `org-cta-buttons` group in
  `src/Organizations.jsx`.
